# Build dockerfile on change
name: Build Docker (env/java.Dockerfile)

on:
  push:
    paths:
    - 'env/java.Dockerfile'
    - '.github/workflows/build_java_docker.yml'
  pull_request:
    paths:
    - 'env/java.Dockerfile'
    - '.github/workflows/build_java_docker.yml'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2

    # Build Tools
    - name: Build and Publish
      uses: elgohr/Publish-Docker-Github-Action@master
      with:
        name: andersenlab/java
        tag: "${{ steps.current-time.formattedTime }}"
        username: ${{ secrets.KSE_DOCKER_USER }}
        password: ${{ secrets.KSE_DOCKER_PASS }}
        snapshot: true
        dockerfile: java.Dockerfile
        workdir: "env"
        tags: "latest"
        cache: true